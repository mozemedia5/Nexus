# Nexus Firebase + Cloudinary environment contract
# Add values in both Preview and Production. Never commit real values or .env files.
# Firebase Console -> Project settings -> Your apps -> Web app configuration.
# Store as one-line JSON; required keys: apiKey, authDomain, projectId, appId.
VITE_FIREBASE_CONFIG_JSON=
# Firebase Console -> Project settings -> Service accounts -> Generate new private key.
# Server-only complete service-account JSON.
FIREBASE_SERVICE_ACCOUNT_JSON=
# Cloudinary server-only signing variables.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# Optional but recommended when using an upload preset.
CLOUDINARY_UPLOAD_PRESET=

# VITE_FIREBASE_CONFIG_JSON is embedded at build time; redeploy after changing it.
# Never prefix Firebase Admin or Cloudinary secrets with VITE_.
# The safe diagnostics endpoint is /api/health/config; it returns booleans only.

# Nexus Production Environment Variables Template for Vercel
# Store these in Vercel Project Settings -> Environment Variables (Production and Preview).

# Shopify Configuration
# Note: SHOPIFY_STORE_DOMAIN, SHOPIFY_STOREFRONT_ACCESS_TOKEN, and SHOPIFY_API_VERSION
# are injected into client bundle via Vite define without requiring a VITE_ prefix.
SHOPIFY_STORE_DOMAIN=
SHOPIFY_STOREFRONT_ACCESS_TOKEN=
SHOPIFY_API_VERSION=2026-07

# Shopify Admin API Token (Server-only for protected order lookup)
SHOPIFY_ADMIN_ACCESS_TOKEN=

# Firebase Client Configuration (The ONLY VITE_-prefixed variable)
# Store as one-line JSON string containing apiKey, authDomain, projectId, appId
VITE_FIREBASE_CONFIG_JSON=

# Server-Only Credentials (NEVER prefix with VITE_)
FIREBASE_SERVICE_ACCOUNT_JSON=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_UPLOAD_PRESET=

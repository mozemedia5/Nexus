import { useEffect, useState } from "react";
import { Link, useLocation } from "wouter";
import { Menu, Search, ShoppingBag, UserRound, X } from "lucide-react";
import { useCart } from "@/contexts/CartContext";
const links = [{ href: "/", label: "Home" }, { href: "/products", label: "Shop all" }, { href: "/products?collection=beauty", label: "Beauty & Personal Care" }, { href: "/products?collection=kitchen", label: "Kitchen & Gadgets" }, { href: "/about", label: "About" }];
export default function SiteHeader() { const [location] = useLocation(); const [menuOpen, setMenuOpen] = useState(false); const { itemCount, openCart } = useCart(); useEffect(() => setMenuOpen(false), [location]); return <><div className="announcement-bar">Smart finds for everyday living <span>·</span> Serving Uganda</div><header className="site-header"><div className="header-inner"><Link href="/" className="brand-lockup" aria-label="Liverton Store home"><span className="brand-mark-wrap">L</span><span className="brand-name">Liverton</span></Link><nav className="desktop-nav" aria-label="Primary navigation">{links.map((link) => <Link key={link.href} href={link.href} className="nav-link">{link.label}</Link>)}</nav><div className="header-actions"><Link href="/products" className="header-icon" aria-label="Search products"><Search size={18} /></Link><button className="cart-trigger" type="button" onClick={openCart} aria-label={`Open shopping bag with ${itemCount} items`}><ShoppingBag size={19} /><span className="cart-label">Bag</span><span className="cart-count">{itemCount}</span></button><button className="menu-trigger" type="button" onClick={() => setMenuOpen(true)} aria-label="Open menu"><Menu size={22} /></button></div></div></header><div className={`mobile-menu ${menuOpen ? "is-open" : ""}`}><div className="mobile-menu-head"><span className="eyebrow">Liverton Store</span><button type="button" className="icon-button" onClick={() => setMenuOpen(false)} aria-label="Close menu"><X size={22} /></button></div><nav className="mobile-nav" aria-label="Mobile navigation">{links.map((link) => <Link key={link.href} href={link.href} className="mobile-nav-link">{link.label}</Link>)}</nav><div className="mobile-menu-foot"><Link href="/account" className="text-link"><UserRound size={15} /> Account</Link><button type="button" className="text-link" onClick={openCart}><ShoppingBag size={15} /> Open your bag</button></div></div>{menuOpen && <button className="mobile-scrim" type="button" aria-label="Close menu" onClick={() => setMenuOpen(false)} />}</>; }
